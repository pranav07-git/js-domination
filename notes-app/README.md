notes app, with CRUD operations and localstorage. 
used event listeners for CRUD operations. 
used localStorage.setItem, & localStorage.getItem() to store and fetch the data locally on the browser storage.  